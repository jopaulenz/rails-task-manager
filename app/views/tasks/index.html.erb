<button><%= link_to 'Add new task', new_tasks_path  %> </button>
<h2>All my tasks!</h2>

<ul class="tasks">
  <% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>
